{
  "name": "nasa-project-api",
  "version": "1.0.0",
  "description": "NASA Mission Control API",
  "main": "src/server.js",
  "scripts": {
    "test": "jest",
    "test-watch": "jest --watch",
    "watch": "nodemon src/server.js",
    // "start": "node src/server.js",
    "deploy": "NODE_OPTIONS=\"--max_old_space_size=8192 --openssl-legacy-provider\" ./node_modules/.bin/netlify-lambda serve src",
     "start": "NODE_OPTIONS=\"--max_old_space_size=8192 --openssl-legacy-provider\" ./node_modules/.bin/netlify-lambda serve src",
     "build": "NODE_OPTIONS=\"--max_old_space_size=8192 --openssl-legacy-provider\" ./node_modules/.bin/netlify-lambda build src",
    "cluster": "pm2 start src/server.js -i max"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^5.4.0",
    "express": "^4.18.2",
    "netlify-lambda": "^2.0.16",
    "pm2": "^5.3.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^29.6.2",
    "morgan": "^1.10.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
